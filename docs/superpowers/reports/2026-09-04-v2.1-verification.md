# codeunlimited 2.1 local release verification

Date: 2026-09-04. Implementation commit: `f15e7d7`.
Branch: `codex/v2.1-subscription-cache`, published to GitHub via existing SSH
authentication. Includes upstream main `1f09563` and its Windows fixes.

## Passed gates

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | Passed |
| `git diff --check` | Passed |
| `cargo clippy --all-targets --all-features --locked -- -D warnings` | Passed |
| `cargo test --release --all-targets --locked --quiet` | 193 Rust tests passed |
| `cargo +1.82.0 check --all-targets --locked` | Passed, including pinned transitive dependencies |
| `python3 -m unittest discover -s tests -p 'test_*.py' -q` | 45 Python tests passed |
| `bash scripts/check_release.sh 2.1` | Passed |
| `cargo package --locked --allow-dirty --no-verify` | 85 files; 777.4 KiB unpacked, 190.8 KiB compressed |
| `CARGO_TARGET_DIR=/Volumes/DevSim/Projects/codeunlimited-v2/target/package-audit bash scripts/audit-package.sh 2.1` | Exact unpacked archive passed all 193 Rust tests |

The archive was built from the source subsequently committed as `f15e7d7`.
These verification and PR notes are documentation-only additions outside the
crate package. The local artifact is `target/package/codeunlimited-2.1.0.crate`;
no crates.io publication is claimed.

An independent read-only review identified redirect credential leakage,
probe integration exposure, provider flag bypasses, failed-output accounting,
and legacy compatibility risks. Fixes and regression tests are included.
Local validation is not a claim that GitHub's cross-platform CI has finished.

## Evidence limits

Tests use deterministic process and loopback HTTP fixtures. No live provider
requests, paid benchmarks, Gemini integration, or subscription quota experiments
were run. Actual token savings, task quality, cache retention, and API model
compatibility remain unmeasured for this release. Cached-input share is not
incremental token savings or recovered subscription quota.

See `docs/VERSION-2.1.md` for capabilities, limits, and tradeoffs.
