Version 2.1 makes subscription CLIs the primary bounded-state runtime path and adds a separate optional API transport. Claude receives additive immutable instructions; Codex keeps its native instructions and an ordered-read bootstrap. Standard preserves integrations; lean is explicit. OpenAI/Anthropic APIs have no local coding tools.

Usage records distinguish cached, newly cached, and ordinary input using provider-native semantics, including counters from failed API output. An opt-in two-call cache probe runs no-op samples in an isolated directory, disables optional integrations, and reports usage separately. A reported cache read is not claimed as incremental token savings or recovered subscription quota.

The change also rejects instruction/continuation overrides at init, disables HTTP redirects, preserves safe Codex tuning and legacy inspection, and includes the Windows fixes and paired experiment data already merged into main.

Validation: 193 Rust tests, 45 Python tests, Clippy with warnings denied, Rust 1.82 all-target check, formatting, and release metadata checks passed locally. Local HTTP/process fixtures only; no live model calls or paid benchmark. See docs/VERSION-2.1.md for capability limits and evidence scope.
