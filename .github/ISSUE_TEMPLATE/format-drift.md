---
name: Log format drift
about: "`codeunlimited doctor` reports unrecognized lines"
labels: format-drift
---

**Doctor output**

```
paste the output of `codeunlimited doctor`
```

**Tool versions**

- Claude Code / Codex CLI version:
- codeunlimited version (`codeunlimited --version`):
- OS:

**Sample (optional, token counts only)**

If you can, paste one unrecognized log line with any text content removed -
keys and token numbers are enough to fix the parser.
